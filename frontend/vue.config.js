const { defineConfig } = require('@vue/cli-service')
module.exports = {
  outputDir: '../static/vue', // This make sure the files are generated in the correct static folder
  publicPath: '/static/vue/',
};