"""Config for nests app."""

from django.apps import AppConfig


class NestsConfig(AppConfig):
    """Config for nests app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "vespadb.nests"
